header
  nav.navbar.navbar-default.navbar-fixed-top
    .container-fluid
      .navbar-header
        button.navbar-toggle(type='button' data-toggle='collapse' data-target='navbar-collapse')
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href='#!/list') SCDB
      .navbar-collapse.collapse
        ul.nav.navbar-nav
          li.active
            a#swTab(href=basePath + '#!/list') Software
          li
            a#instTab(href=basePath + '#!/inst/list') Installations
        //- ul.nav.navbar-nav.navbar-right
          li
            span.fa.fa-user.fa-3x
          li
            a#usrBtn(ng-click='usrBtnClk($event)' href='#')
              | {{session.user.uid}} {{usrBtnTxt}}
